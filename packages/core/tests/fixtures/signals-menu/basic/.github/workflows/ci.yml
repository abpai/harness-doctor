name: CI

on:
  pull_request:

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - run: pnpm build
  test:
    runs-on: ubuntu-latest
    steps:
      - run: pnpm test
      - name: checks
        run: |
          pnpm lint
          pnpm typecheck
