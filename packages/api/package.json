{
  "name": "@harness-doctor/api",
  "version": "0.2.0",
  "private": true,
  "description": "Internal: programmatic diagnose() API for Harness Doctor — thin Effect.runPromise shell around @harness-doctor/core's runInspect orchestrator. Not published.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && cross-env NODE_ENV=production vp pack",
    "test": "vp test run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@harness-doctor/core": "workspace:*",
    "effect": "4.0.0-beta.70"
  },
  "devDependencies": {
    "@types/node": "^25.6.0"
  },
  "engines": {
    "bun": ">=1.3.14"
  }
}
